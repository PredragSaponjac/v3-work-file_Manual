"""Stub: Predictive ML nowcast proxy. Phase 4+."""

def nowcast(ticker: str, horizon_days: int = 5) -> dict:
    """Returns empty forecast. Future: ML-based short-term prediction."""
    return {"ticker": ticker, "horizon_days": horizon_days, "forecast": None, "stub": True}
